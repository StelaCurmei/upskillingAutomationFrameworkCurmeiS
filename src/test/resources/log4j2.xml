<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <!-- Maven sets this to 'target' when you run via mvn; otherwise we default to 'target' -->
        <Property name="buildDir">${sys:project.build.directory:-target}</Property>
        <Property name="logsDir">${buildDir}/logs</Property>
    </Properties>

    <Appenders>
        <!-- Console Logger -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>

        <!-- Flat file for per-test-suite overview -->
        <File name="FileLogger"
              fileName="${logsDir}/${ctx:testName}/test-execution.log"
              createDirs="true">
            <PatternLayout
                    pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
        </File>

        <!-- Routing appender for individual test-case logs -->
        <Routing name="ScenarioLogger">
            <Routes pattern="$${ctx:testName}">
                <Route>
                    <File name="PerTestLogger"
                          fileName="${logsDir}/${ctx:testTime}/${ctx:testName}.log"
                          append="true"
                          createDirs="true">
                        <PatternLayout
                                pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
                    </File>
                </Route>
            </Routes>
        </Routing>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="FileLogger"/>
            <AppenderRef ref="ScenarioLogger"/>
        </Root>
    </Loggers>
</Configuration>